# Frontend public config. Copy to .env for local dev.
# This is the deployed waitlist proxy URL that receives form submissions.
# It is NOT secret (the browser calls it directly) — the Airtable token lives
# only inside the proxy service (server/), set via Render env vars.
VITE_WAITLIST_ENDPOINT=https://clarivance-waitlist.onrender.com
