# Frontend public config. Copy to .env for local dev.
# This is the waitlist proxy URL that receives form submissions.
# It points to the Vercel Serverless Function (/api/waitlist).
# The Airtable token lives securely in Vercel environment variables.
VITE_WAITLIST_ENDPOINT=/api/waitlist
